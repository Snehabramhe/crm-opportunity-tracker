# Base URL of the backend API.
# Local dev: leave as /api (Vite proxies to the backend on port 5000).
# Production: set to your deployed backend, e.g. https://your-backend.onrender.com/api
VITE_API_URL=/api
